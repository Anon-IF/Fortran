# 配套练习题
## ——《工程分析程序设计》各章练习

> 💡 学习建议：先自己独立思考和动手写代码，实在不会再看提示。
> 每道题都先把程序写出来、运行成功后，再去做下一题。

---

## 第1章练习：程序的基本结构

### 练习 1-1（★☆☆ 入门）
**题目**：修改 `hello.f90`，让程序输出你自己的名字和当前日期。

**期望输出示例**：
```
 你好！我叫张三
 今天是 2025 年 3 月
 开始我的 Fortran 学习之旅！
```

**提示**：`print *, "文字"` 可以输出一行文字，写三个 `print` 就能输出三行。

---

### 练习 1-2（★☆☆ 入门）
**题目**：写一个程序，计算并输出 1+2+3 的结果，以及 10×5 的结果。

**期望输出**：
```
 1 + 2 + 3 = 6
 10 * 5 = 50
```

**提示**：`print *, "1 + 2 + 3 =", 1+2+3` 可以直接在 `print` 里算。

---

## 第2章练习：变量与数据类型

### 练习 2-1（★☆☆ 入门）
**题目**：声明变量 `length`（长）、`width`（宽），赋值后计算并输出矩形的**面积**和**周长**。

**参考思路**：
```
面积 = 长 × 宽
周长 = 2 × (长 + 宽)
```

---

### 练习 2-2（★★☆ 进阶）
**题目**：从键盘读入摄氏温度，转换为华氏温度并输出。

**公式**：华氏温度 = 摄氏温度 × 9/5 + 32

**注意**：`9/5` 是整数除法，结果是 1！应该写成 `9.0/5.0`。

**运行示例**：
```
请输入摄氏温度：
100
100.00 摄氏度 = 212.00 华氏度
```

---

### 练习 2-3（★★☆ 进阶）
**题目**：声明一个双精度变量，计算并输出 π 的近似值（用 4×arctan(1) 计算）。

**提示**：Fortran 中反正切函数是 `atan(1.0_8)`，结果为 π/4。

---

## 第3章练习：流程控制

### 练习 3-1（★☆☆ 入门）
**题目**：读入一个整数，判断它是否能被 3 整除，能被 5 整除，或同时能被 3 和 5 整除。

**提示**：取余运算 `mod(n, 3) == 0` 表示 n 能被 3 整除。

**运行示例**：
```
请输入一个整数：15
15 能同时被 3 和 5 整除（是 FizzBuzz！）
```

---

### 练习 3-2（★★☆ 进阶）
**题目**：用 `do` 循环计算 1² + 2² + 3² + ... + n²（n 由键盘输入）。

**运行示例**：
```
请输入 n：5
1^2 + 2^2 + ... + 5^2 = 55
```

---

### 练习 3-3（★★☆ 进阶）
**题目**：打印如下图形（用 `*` 号）：

```
*
**
***
****
*****
```

**提示**：外层循环控制行数，内层循环控制每行 `*` 的个数。注意 `print *, "*"` 每次会换行，需要用格式化输出 `write(*,"(a)", advance="no") "*"` 来实现不换行输出。

---

### 练习 3-4（★★★ 挑战）
**题目**：写一个猜数字游戏程序：
1. 程序设定一个"秘密数字"（比如 42）
2. 用户不断猜测
3. 程序告诉用户"猜大了"或"猜小了"
4. 直到猜中为止，输出猜了几次

**运行示例**：
```
猜猜我想的数字（1-100）：50
猜大了！
猜猜我想的数字（1-100）：25
猜小了！
猜猜我想的数字（1-100）：42
猜对了！你用了 3 次猜中了！
```

---

## 第4章练习：数组

### 练习 4-1（★☆☆ 入门）
**题目**：声明一个有 10 个元素的整数数组，用循环赋值（第 i 个元素的值为 i²），然后输出所有元素和它们的总和。

---

### 练习 4-2（★★☆ 进阶）
**题目**：从键盘读入 5 个实数，存入数组，然后：
1. 输出最大值和最小值
2. 计算并输出平均值
3. 将数组元素按从小到大排序后输出（冒泡排序）

**冒泡排序提示**：
```fortran
do i = 1, n-1
  do j = 1, n-i
    if (a(j) > a(j+1)) then
      ! 交换 a(j) 和 a(j+1)
    end if
  end do
end do
```

---

### 练习 4-3（★★★ 挑战）
**题目**：定义一个 3×3 矩阵 A，赋初值，然后：
1. 输出矩阵 A
2. 计算矩阵 A 的转置矩阵 B（用 `transpose` 函数或手动实现）
3. 计算 A×B 的矩阵乘法（用 `matmul` 函数或手动实现）
4. 输出结果

---

## 第5章练习：函数与子例程

### 练习 5-1（★★☆ 进阶）
**题目**：写一个函数 `power(base, n)`，计算 base 的 n 次方（不用 `**` 运算符，用循环实现）。

---

### 练习 5-2（★★☆ 进阶）
**题目**：写一个子例程 `sort_ascending(arr, n)`，对长度为 n 的实数数组 `arr` 进行升序排序（原地修改）。在主程序中测试它。

---

### 练习 5-3（★★★ 挑战）
**题目**：写一个函数 `fibonacci(n)`，计算斐波那契数列的第 n 项（f(1)=1, f(2)=1, f(n)=f(n-1)+f(n-2)）。在主程序中输出前 20 项。

**期望输出**：
```
 F(1) = 1
 F(2) = 1
 F(3) = 2
 F(4) = 3
 F(5) = 5
 ...
```

---

## 第6章练习：模块

### 练习 6-1（★★☆ 进阶）
**题目**：创建一个 `statistics_module` 模块，包含以下功能：
- 函数 `mean(arr, n)`：计算平均值
- 函数 `variance(arr, n)`：计算方差
- 函数 `std_dev(arr, n)`：计算标准差

在主程序中读入一组数据（从键盘输入），调用这些函数并输出结果。

---

## 第7章练习：文件读写

### 练习 7-1（★★☆ 进阶）
**题目**：写一个程序，将 1 到 100 中所有能被 7 整除的数写入文件 `result.txt`，一行一个数。运行后用文本编辑器打开文件验证结果。

---

### 练习 7-2（★★★ 挑战）
**题目**：创建一个文件 `students.txt`，内容格式为"姓名 成绩"（如 `张三 85`），写一个程序：
1. 读入文件中所有学生的成绩
2. 计算班级平均分
3. 找出最高分和最低分
4. 将高于平均分的学生名单写入 `top_students.txt`

---

## 第8章练习：数值方法

### 练习 8-1（★★☆ 进阶）
**题目**：用二分法（Bisection Method）求方程 `f(x) = x² - 2 = 0` 在区间 [1, 2] 内的根（即求 √2 的近似值）。

**二分法原理**：
- 初始区间 [a, b]，令 m = (a+b)/2
- 若 f(m) × f(a) < 0，说明根在 [a, m]，令 b = m
- 否则根在 [m, b]，令 a = m
- 重复直到 |b - a| < 容限

---

### 练习 8-2（★★★ 挑战）
**题目**：用梯形法计算以下定积分，并与解析解对比：

∫₀^π sin(x) dx = 2

分别取 n = 10, 100, 1000 段，观察误差如何随 n 的增大而减小。

---

## 综合项目：抛体运动轨迹计算

**题目**：编写一个程序，模拟炮弹的抛体运动：

**物理模型**：
- 初速度 v₀，发射角度 θ
- x(t) = v₀ cos(θ) × t
- y(t) = v₀ sin(θ) × t - ½gt²
- 其中 g = 9.8 m/s²

**要求**：
1. 从键盘读入初速度（m/s）和发射角度（度）
2. 计算飞行时间（y = 0 时落地）
3. 计算最大射程和最高点
4. 将轨迹数据（t, x, y）写入文件 `trajectory.txt`，每 0.1 秒记录一次
5. 输出最大射程和最高高度

**参考输出**：
```
请输入初速度（m/s）：50
请输入发射角度（度）：45
飞行时间 = 7.21 秒
最大射程 = 255.10 米
最高点高度 = 63.78 米
轨迹已写入 trajectory.txt
```

---

> 📝 **做完题目后**：
> - 尝试修改程序，看看会有什么变化
> - 思考有没有更简单或更高效的写法
> - 把你的解答与同学分享，互相学习
